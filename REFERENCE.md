# Reference

<!-- DO NOT EDIT: This document was generated by Puppet Strings -->

## Table of Contents

### Classes

#### Public Classes

* [`codavox`](#codavox): Installs codavox and manages its configuration file.
* [`codavox::agent`](#codavox--agent): Runs the codavox agent, converging this compiler onto the publisher.
* [`codavox::deploy_server`](#codavox--deploy_server): Runs the codavox deploy server: the deploy API and control-repo webhook.
* [`codavox::publish`](#codavox--publish): Runs the codavox publisher, which serves versioned code to compilers.
* [`codavox::server`](#codavox--server): Points OpenVox Server at codavox, enabling correct static catalogs.

#### Private Classes

* `codavox::config`: Writes codavox's configuration file. Private.
* `codavox::install`: Installs the codavox package. Private.

## Classes

### <a name="codavox"></a>`codavox`

codavox distributes versioned Puppet code to OpenVox compilers. It implements
OpenVox Server's already-shipped versioned-code-service contract, so the server
needs no patching: each compiler answers "which exact version am I serving?"
from the same symlink the server resolves, which turns divergence between
compilers from a correctness bug into a latency property.

This class installs the package and writes the configuration file. It starts
nothing. Which daemon a node runs is decided by including a role class, because
that belongs to the node's role rather than to the act of installing software:

* `codavox::publish` — serve versioned code to compilers, on the primary
* `codavox::agent` — converge this compiler onto the publisher
* `codavox::deploy_server` — the deploy API and control-repo webhook
* `codavox::server` — point OpenVox Server at codavox, on each compiler

Every setting arrives here. The role classes read these values back rather
than taking duplicates of their own, so each one has a single source of truth.

#### Examples

##### Install the package and write the config, start nothing

```puppet
include codavox
```

##### A primary that publishes, driven from Hiera

```puppet
codavox::package_source: 'https://github.com/miharp/codavox/releases/download/v0.2.1/codavox_0.2.1_linux_amd64.rpm'
codavox::staging: '/etc/puppetlabs/code-staging'
```

#### Parameters

The following parameters are available in the `codavox` class:

* [`package_name`](#-codavox--package_name)
* [`package_ensure`](#-codavox--package_ensure)
* [`config_file`](#-codavox--config_file)
* [`environmentpath`](#-codavox--environmentpath)
* [`package_source`](#-codavox--package_source)
* [`package_provider`](#-codavox--package_provider)
* [`package_manage`](#-codavox--package_manage)
* [`config_manage`](#-codavox--config_manage)
* [`staging`](#-codavox--staging)
* [`state`](#-codavox--state)
* [`ssldir`](#-codavox--ssldir)
* [`certname`](#-codavox--certname)
* [`r10k`](#-codavox--r10k)
* [`r10k_config`](#-codavox--r10k_config)
* [`publish_listen`](#-codavox--publish_listen)
* [`publish_allow_roles`](#-codavox--publish_allow_roles)
* [`publish_certificate_revocation`](#-codavox--publish_certificate_revocation)
* [`agent_publisher`](#-codavox--agent_publisher)
* [`agent_interval`](#-codavox--agent_interval)
* [`agent_keep`](#-codavox--agent_keep)
* [`agent_min_age`](#-codavox--agent_min_age)
* [`agent_prune_environments`](#-codavox--agent_prune_environments)
* [`deploy_server_listen`](#-codavox--deploy_server_listen)
* [`deploy_server_api_token_file`](#-codavox--deploy_server_api_token_file)
* [`deploy_server_secret_file`](#-codavox--deploy_server_secret_file)
* [`deploy_server_api_token`](#-codavox--deploy_server_api_token)
* [`deploy_server_secret`](#-codavox--deploy_server_secret)
* [`deploy_server_history`](#-codavox--deploy_server_history)

##### <a name="-codavox--package_name"></a>`package_name`

Data type: `String[1]`

The package to install.

Default value: `'codavox'`

##### <a name="-codavox--package_ensure"></a>`package_ensure`

Data type: `String[1]`

A version to pin, or `installed`, `latest`, or `absent`.

Default value: `'installed'`

##### <a name="-codavox--config_file"></a>`config_file`

Data type: `Stdlib::Absolutepath`

Path to the configuration file the daemons read.

Default value: `'/etc/codavox/config.yaml'`

##### <a name="-codavox--environmentpath"></a>`environmentpath`

Data type: `Stdlib::Absolutepath`

The directory codavox owns and fills with environment symlinks, and which
OpenVox Server is pointed at. Deliberately not the stock `code/environments`:
a fresh OpenVox Server ships a populated directory there, and `rename(2)`
cannot replace a real directory with a symlink.

Default value: `'/opt/puppetlabs/codavox/environments'`

##### <a name="-codavox--package_source"></a>`package_source`

Data type: `Optional[String[1]]`

Install from this file or URL instead of from a repository. codavox publishes
to GitHub Releases and runs no package repository, so this is the ordinary
case rather than the exception. A direct package install resolves no
dependencies; codavox is a static binary and has none.

Default value: `undef`

##### <a name="-codavox--package_provider"></a>`package_provider`

Data type: `Optional[String[1]]`

Provider to use when `package_source` is set. Defaults per OS family from
this module's data.

Default value: `undef`

##### <a name="-codavox--package_manage"></a>`package_manage`

Data type: `Boolean`

Whether to manage the package. Set to false when something else installs it.

Default value: `true`

##### <a name="-codavox--config_manage"></a>`config_manage`

Data type: `Boolean`

Whether to manage the configuration file. The package ships it as a
noreplace conffile with every setting commented out, so an upgrade never
overwrites what Puppet writes here.

Default value: `true`

##### <a name="-codavox--staging"></a>`staging`

Data type: `Optional[Stdlib::Absolutepath]`

r10k's basedir: the tree the publisher seals. Required by `codavox::publish`
and `codavox::deploy_server`, and it must be the same directory r10k deploys
into.

Default value: `undef`

##### <a name="-codavox--state"></a>`state`

Data type: `Optional[Stdlib::Absolutepath]`

Where the publisher keeps materialized artifacts, its pidfile, and the
provenance log.

Default value: `undef`

##### <a name="-codavox--ssldir"></a>`ssldir`

Data type: `Optional[Stdlib::Absolutepath]`

Puppet's ssldir. codavox reuses the certificate the node already holds and
issues none of its own, so there is no second PKI to rotate or revoke.

Default value: `undef`

##### <a name="-codavox--certname"></a>`certname`

Data type: `Optional[Stdlib::Host]`

This node's certname. Left unset, codavox uses the node's hostname.

Default value: `undef`

##### <a name="-codavox--r10k"></a>`r10k`

Data type: `Optional[Stdlib::Absolutepath]`

Path to the r10k binary, for `codavox::deploy_server`.

Default value: `undef`

##### <a name="-codavox--r10k_config"></a>`r10k_config`

Data type: `Optional[Stdlib::Absolutepath]`

Path to r10k.yaml, for `codavox::deploy_server`.

Default value: `undef`

##### <a name="-codavox--publish_listen"></a>`publish_listen`

Data type: `Optional[String[1]]`

Address the publisher listens on.

Default value: `undef`

##### <a name="-codavox--publish_allow_roles"></a>`publish_allow_roles`

Data type: `Optional[Array[String[1], 1]]`

The `pp_role` values permitted to fetch code. A certificate signed by the
Puppet CA proves only that the peer is *some* enrolled node, and every agent
in the estate clears that bar, so the role is what actually authorizes.

A primary that serves its own catalogs is also a client of its own
publisher, so its role has to appear here too — ovadm gives a primary
`openvox_server` and a compiler `openvox_compiler`.

Default value: `undef`

##### <a name="-codavox--publish_certificate_revocation"></a>`publish_certificate_revocation`

Data type: `Optional[Enum['chain', 'leaf', 'false']]`

Whether the publisher refuses revoked certificates, read from
`<ssldir>/crl.pem`. Takes Puppet's own values: `chain`, `leaf`, or `false`.
Left unset, codavox defaults to `chain`, as puppetserver does. Set `false`
only where no CRL is distributed.

Default value: `undef`

##### <a name="-codavox--agent_publisher"></a>`agent_publisher`

Data type: `Optional[Stdlib::HTTPUrl]`

The publisher's base URL. Required by `codavox::agent`.

Default value: `undef`

##### <a name="-codavox--agent_interval"></a>`agent_interval`

Data type: `Optional[String[1]]`

How often the agent polls, as a Go duration such as `30s`.

Default value: `undef`

##### <a name="-codavox--agent_keep"></a>`agent_keep`

Data type: `Optional[Integer[1]]`

How many superseded versions to retain per environment.

Default value: `undef`

##### <a name="-codavox--agent_min_age"></a>`agent_min_age`

Data type: `Optional[String[1]]`

How long a superseded version is retained regardless of `agent_keep`. This
is the guard that matters: an agent run holding a catalog stamped with an
older `code_id` still requests file content for it.

Default value: `undef`

##### <a name="-codavox--agent_prune_environments"></a>`agent_prune_environments`

Data type: `Optional[Boolean]`

Whether to remove environments the publisher no longer serves. Off by
default because deletion is destructive; needs r10k's `purge_levels` set to
match.

Default value: `undef`

##### <a name="-codavox--deploy_server_listen"></a>`deploy_server_listen`

Data type: `Optional[String[1]]`

Address the deploy server listens on.

Default value: `undef`

##### <a name="-codavox--deploy_server_api_token_file"></a>`deploy_server_api_token_file`

Data type: `Optional[Stdlib::Absolutepath]`

File holding the deploy API bearer token. Setting it enables the API.

Default value: `undef`

##### <a name="-codavox--deploy_server_secret_file"></a>`deploy_server_secret_file`

Data type: `Optional[Stdlib::Absolutepath]`

File holding the webhook shared secret. Setting it enables the webhook.

Default value: `undef`

##### <a name="-codavox--deploy_server_api_token"></a>`deploy_server_api_token`

Data type: `Optional[Sensitive[String[1]]]`

Contents for `deploy_server_api_token_file`. Left unset, the file is assumed
to be managed elsewhere; codavox will not start if it names a file that does
not exist.

Default value: `undef`

##### <a name="-codavox--deploy_server_secret"></a>`deploy_server_secret`

Data type: `Optional[Sensitive[String[1]]]`

Contents for `deploy_server_secret_file`, on the same terms.

Default value: `undef`

##### <a name="-codavox--deploy_server_history"></a>`deploy_server_history`

Data type: `Optional[Integer[1]]`

How many past deploys to keep in the queryable history.

Default value: `undef`

### <a name="codavox--agent"></a>`codavox::agent`

Include this on each compiler. The agent polls the publisher, fetches any
artifact it does not already hold, verifies it by resealing the unpacked tree —
proving the content is what the `code_id` names, not merely that the bytes
arrived intact — and then swaps the environment symlink with a single
`rename(2)`.

Compilers poll; nothing is pushed to them. That is the property the project
exists for: a compiler that was down during a deploy catches up on its own next
poll, with no event to replay and no operator intervention. It also means a
publisher outage degrades to "no new deploys" rather than "no catalogs".

Requires `codavox::agent_publisher`.

Include `codavox::server` as well to point OpenVox Server at the code this
deploys. Order matters on a first run — see that class.

#### Examples

##### On a compiler, with the publisher set in Hiera

```puppet
include codavox::agent
```

#### Parameters

The following parameters are available in the `codavox::agent` class:

* [`service_name`](#-codavox--agent--service_name)
* [`service_ensure`](#-codavox--agent--service_ensure)
* [`service_enable`](#-codavox--agent--service_enable)
* [`service_manage`](#-codavox--agent--service_manage)

##### <a name="-codavox--agent--service_name"></a>`service_name`

Data type: `String[1]`

The systemd service the package ships.

Default value: `'codavox-agent'`

##### <a name="-codavox--agent--service_ensure"></a>`service_ensure`

Data type: `Stdlib::Ensure::Service`

Whether the service should be running.

Default value: `'running'`

##### <a name="-codavox--agent--service_enable"></a>`service_enable`

Data type: `Boolean`

Whether the service starts at boot.

Default value: `true`

##### <a name="-codavox--agent--service_manage"></a>`service_manage`

Data type: `Boolean`

Whether to manage the service at all.

Default value: `true`

### <a name="codavox--deploy_server"></a>`codavox::deploy_server`

Include this on the primary, alongside `codavox::publish`. It gives two front
doors onto one deploy queue: a token-authenticated API for CI, and a
secret-authenticated webhook for a push from GitHub, GitLab, or a generic
caller. However a deploy is triggered it takes the same path and lands in the
same history.

Unlike the publisher, its callers cannot present a Puppet certificate, so it
authenticates a bearer token or a shared secret instead of mutual TLS. At least
one of `codavox::deploy_server_api_token_file` or
`codavox::deploy_server_secret_file` must be set; each enables its own route,
and codavox refuses to start with neither.

The token and secret files can be managed here by setting
`codavox::deploy_server_api_token` and `codavox::deploy_server_secret` — in a
control repository those come from eyaml. Left unset, the files are assumed to
be managed elsewhere.

#### Examples

##### Webhook only, with the secret from eyaml

```puppet
codavox::deploy_server_secret_file: '/etc/codavox/webhook.secret'
codavox::deploy_server_secret: >
  ENC[PKCS7,...]
```

#### Parameters

The following parameters are available in the `codavox::deploy_server` class:

* [`service_name`](#-codavox--deploy_server--service_name)
* [`service_ensure`](#-codavox--deploy_server--service_ensure)
* [`service_enable`](#-codavox--deploy_server--service_enable)
* [`service_manage`](#-codavox--deploy_server--service_manage)

##### <a name="-codavox--deploy_server--service_name"></a>`service_name`

Data type: `String[1]`

The systemd service the package ships.

Default value: `'codavox-deploy-server'`

##### <a name="-codavox--deploy_server--service_ensure"></a>`service_ensure`

Data type: `Stdlib::Ensure::Service`

Whether the service should be running.

Default value: `'running'`

##### <a name="-codavox--deploy_server--service_enable"></a>`service_enable`

Data type: `Boolean`

Whether the service starts at boot.

Default value: `true`

##### <a name="-codavox--deploy_server--service_manage"></a>`service_manage`

Data type: `Boolean`

Whether to manage the service at all.

Default value: `true`

### <a name="codavox--publish"></a>`codavox::publish`

Include this on the node holding r10k's staging directory — normally the
primary. The publisher seals each staged environment into a content-addressed
`code_id`, materializes an immutable artifact for it, and serves both to
compilers over mutual TLS using the Puppet certificate the node already has.

Sealing happens when this service starts and on `SIGHUP`, not per request:
walking and hashing an environment is far too expensive to repeat for every
polling compiler, and two compilers polling either side of an r10k run would
otherwise see different ids for one deploy. `systemctl reload codavox-publish`
sends that signal, which is what r10k's `postrun` hook should call.

Requires `codavox::staging`. Set `codavox::publish_allow_roles` to the
`pp_role` values allowed to fetch code — including this node's own role if it
also serves its own catalogs.

#### Examples

##### On a primary, with staging and the roles set in Hiera

```puppet
include codavox::publish
```

#### Parameters

The following parameters are available in the `codavox::publish` class:

* [`service_name`](#-codavox--publish--service_name)
* [`service_ensure`](#-codavox--publish--service_ensure)
* [`service_enable`](#-codavox--publish--service_enable)
* [`service_manage`](#-codavox--publish--service_manage)

##### <a name="-codavox--publish--service_name"></a>`service_name`

Data type: `String[1]`

The systemd service the package ships.

Default value: `'codavox-publish'`

##### <a name="-codavox--publish--service_ensure"></a>`service_ensure`

Data type: `Stdlib::Ensure::Service`

Whether the service should be running.

Default value: `'running'`

##### <a name="-codavox--publish--service_enable"></a>`service_enable`

Data type: `Boolean`

Whether the service starts at boot.

Default value: `true`

##### <a name="-codavox--publish--service_manage"></a>`service_manage`

Data type: `Boolean`

Whether to manage the service at all.

Default value: `true`

### <a name="codavox--server"></a>`codavox::server`

Include this on each compiler, alongside `codavox::agent`. It writes
`versioned-code.conf` naming codavox's two commands, turns static catalogs on,
and points `environmentpath` at the directory codavox fills with symlinks.

This is what replaces a hand-rolled pair of shell scripts. The difference is
not tidiness. A script that answers `code_id` from a timestamp when it cannot
find a git signature gives every compile a new version, and one that answers
`code_content` by reading the file from the current tree when it cannot find
the requested commit serves content from a *different version than the catalog
was compiled against* while exiting zero. That is the exact failure static
catalogs exist to prevent, and it is silent. codavox has no such fallback: an
undeployed `code_id` is a hard error.

## Ordering on a first run

Repointing `environmentpath` at a directory the agent has not filled yet leaves
OpenVox Server with no environments, and **every catalog compile fails** —
including the run making this change, on a primary that manages itself.

This class orders itself after `codavox::agent` when both are included, so the
agent is installed and started first. That narrows the window but does not close
it: the agent converges asynchronously, so a first run can still repoint the
server before the first environment lands. On a node that compiles its own
catalog, do the cutover in two steps — apply with `enabled => false`, confirm
`codavox code-id production` answers, then set it true.

#### Examples

##### Wire it up, with everything else in Hiera

```puppet
include codavox::agent
include codavox::server
```

##### Stage the cutover on a self-compiling primary

```puppet
class { 'codavox::server':
  manage_environmentpath => false,
}
```

#### Parameters

The following parameters are available in the `codavox::server` class:

* [`enabled`](#-codavox--server--enabled)
* [`code_id_command`](#-codavox--server--code_id_command)
* [`code_content_command`](#-codavox--server--code_content_command)
* [`manage_environmentpath`](#-codavox--server--manage_environmentpath)
* [`manage_static_catalogs`](#-codavox--server--manage_static_catalogs)
* [`puppet_config`](#-codavox--server--puppet_config)
* [`puppetserver_confdir`](#-codavox--server--puppetserver_confdir)
* [`service_name`](#-codavox--server--service_name)
* [`service_manage`](#-codavox--server--service_manage)

##### <a name="-codavox--server--enabled"></a>`enabled`

Data type: `Boolean`

Whether OpenVox Server is pointed at codavox. Setting this false removes the
settings again, so a control repository can flip between codavox and whatever
it used before without deleting code.

Default value: `true`

##### <a name="-codavox--server--code_id_command"></a>`code_id_command`

Data type: `Stdlib::Absolutepath`

Path to the `code_id` command. The package ships this as a symlink, because
OpenVox Server passes only positional arguments and so cannot invoke
`codavox code-id <env>`.

Default value: `'/usr/bin/codavox-code-id'`

##### <a name="-codavox--server--code_content_command"></a>`code_content_command`

Data type: `Stdlib::Absolutepath`

Path to the `code_content` command, on the same terms.

Default value: `'/usr/bin/codavox-code-content'`

##### <a name="-codavox--server--manage_environmentpath"></a>`manage_environmentpath`

Data type: `Boolean`

Whether to set `environmentpath`. Leave this false to stage a cutover: the
commands are wired but the server keeps serving its existing code.

Default value: `true`

##### <a name="-codavox--server--manage_static_catalogs"></a>`manage_static_catalogs`

Data type: `Boolean`

Whether to set `static_catalogs`. It defaults to true in OpenVox Server, so
this is usually already the case.

Default value: `true`

##### <a name="-codavox--server--puppet_config"></a>`puppet_config`

Data type: `Stdlib::Absolutepath`

Path to puppet.conf.

Default value: `'/etc/puppetlabs/puppet/puppet.conf'`

##### <a name="-codavox--server--puppetserver_confdir"></a>`puppetserver_confdir`

Data type: `Stdlib::Absolutepath`

Directory holding OpenVox Server's HOCON configuration.

Default value: `'/etc/puppetlabs/puppetserver/conf.d'`

##### <a name="-codavox--server--service_name"></a>`service_name`

Data type: `String[1]`

The OpenVox Server service to restart when the wiring changes.

Default value: `'puppetserver'`

##### <a name="-codavox--server--service_manage"></a>`service_manage`

Data type: `Boolean`

Whether to declare that service here. Set this false where another class
already manages it with attributes of its own, which would otherwise be a
duplicate declaration.

The restart is wired through a resource collector either way, so it happens
whenever the service is declared — here or elsewhere — and is skipped
silently when nothing manages it at all.

Default value: `true`

